class BaseUser {
  name: string;
}

// создать два новых классов Admin и Guest, которые должные быть наследниками класс BaseUser


// типизровать функцию asArray у которой взаимосвязаны типы входящего параметра и возврааемого значения
const asArray = (value) => [ value ];


interface Car {
  id:
}

getByKey

Partial
Required
Pick
  Picl + intersection
Omit
Exlude
Extract
ReturnType / Parameters
NonNullable
  Сделать свой nullable
